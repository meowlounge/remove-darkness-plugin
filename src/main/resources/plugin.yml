name: remove-darkness
version: '${version}'
author: "prodbyeagle"
prefix: "EAGLE: RD"
main: dev.prodbyeagle.removeDarknessPlugin.RemoveDarknessPlugin
api-version: '1.21.10'
commands:
  darkness:
    description: Toggle whether darkness effects are removed for you.
    usage: /<command> [allow|deny|toggle|status]
